{
  "name": "biztrack",
  "version": "1.0.0",
  "private": true,
  "description": "Small-business dashboard with accessible, multilingual, privacy-aware enhancements.",
  "scripts": {
    "test": "vitest run --coverage",
    "test:watch": "vitest",
    "serve": "vite --host 127.0.0.1"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^4.1.5",
    "vite": "^8.0.12",
    "vitest": "^4.1.5"
  }
}
